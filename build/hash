#!/bin/bash

set -e
set -o pipefail
hash=$(find src/*/src/*/policy/theme -type f | sort | tr '\n' '\0' | xargs -0 cat | (grep -av '..unique..' || true) | md5sum - | awk ' { print $1 } ')

echo "$hash"