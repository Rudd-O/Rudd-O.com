#!/bin/bash
DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
cd "$DIR"
kwrite  src/manuelamador/policy/configure.zcml src/manuelamador/policy/profiles/default/metadata.xml setup.py src/manuelamador/policy/setuphandlers.py
